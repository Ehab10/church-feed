{
  "name": "church-feed",
  "private": true,
  "version": "1.0.0",
  "description": "Generates an RSS feed (feed.xml) from Markdown posts for pulling into ChMeetings.",
  "type": "commonjs",
  "scripts": {
    "build": "node scripts/build-feed.js"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^12.0.2"
  }
}
